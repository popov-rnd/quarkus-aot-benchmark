quarkus:
  application:
    name: quarkus-aot-benchmark
  package:
    jar:
      type: fast-jar # uber-jar
  #thread-pool:
    #max-threads: 20
  rest-client:
    connection-pool-size: 1000 #50 by default

  log:
    level: ${LOG_LEVEL_ROOT:WARN}
    category:
      "com.popovrnd":
        level: ${LOG_LEVEL_BENCHMARK:WARN}